# Keyboard shortcuts
## Yazi
- `Z` - dir using zoxide
- `Space` - visual selection
- `y` - copy
- `x` - cut
- `p` - paste
- `a` - new file or dir
- `:` - shell command

## Vim
- editing multiple lines: `ctrl+v -> j (or G for end) -> I -> type, esc `
