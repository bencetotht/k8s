handle:
  storageClass: nfs
replication:
  web: 2
  handle: 2
loki:
  commonConfig:
    replication_factor: 1
  storage:
    type: 'filesystem'
singleBinary:
  replicas: 1
versions:
  handle_tag: latest
  web_tag: latest